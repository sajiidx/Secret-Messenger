import * as actions from "../constants";

const initialState = {
}

export const store = (state = initialState, action) => {
    switch (action.type) {
        default:
            return state;
    }
}